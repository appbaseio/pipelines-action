description: "New description from GitHub Action"
priority: 1
routes:
  - path: "/action/test"
    method: POST
    classify:
      category: "search"
      acl: "search"
envs:
  RESOLVED_KEY: ${{ TEST_KEY }}


global_envs:
  - key: "SOME_ENV_KEY"
    value: ${{ TEST_KEY }}
    label: "This is a test key as global env"

stages:
  - id: "nana"
    script: "console.log('nana');"
  - id: "second step"
    scriptRef: "test.js"
  - id: "third step"
    envs:
      SECRET_VALUE: ${{ TEST_KEY }}
    scriptRef: "./test.js"
  - id: "fourht"
    scriptRef: "/test.js"